[project]
name = "rag"
version = "0.1.0"
description = "RAG system"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "langchain>=1.3.14",
    "langchain-openai>=1.4.0",
    "langchain-text-splitters>=1.1.2",
]
